# P0 Bug 修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 baseline 报告中的 13 个 P0 Bug，将测试通过率从 50% 提升到 86%+。

**Architecture:**
- 后端：异常体系统一（`ai.LLMError` → `core.AppException`）、评改字段读取从 `CheckinRecord` 迁移到 join `Grade` 表、Camp 校验改为 warning 模式、Checkin 提交加同日去重。
- 前端：学员/志愿者 API 路径加 `/student` `/volunteer` 前缀；Dashboard 档案链接补 camp 段；不再依赖后端 alias。
- 不修改部署脚本（uvicorn 单 worker 由部署侧处理），不引入新依赖。

**Tech Stack:** FastAPI / SQLAlchemy 2.0 / Pydantic v2 / React + TypeScript + Vite + axios + React Query

**前置文档:**
- 测试基线：`test-reports/00-baseline-bug-report.md`
- 详细报告：`test-reports/agent{1-5}-*.md`

---

## 任务编排

| 阶段 | 任务数 | 风险 | 说明 |
| --- | --- | --- | --- |
| **P0-A** 后端异常体系 | 1 | 低 | 修复 NFR-001，AI 失败统一返回 3001 |
| **P0-B** 后端评改字段修复 | 1 | 中 | 修复 VOL-002/003，迁移 comment 读取路径 |
| **P0-C** 后端 Camp 校验 | 2 | 低 | 修复 CAMP-005/006，0.6 兜底 + 倒序不阻断 |
| **P0-D** 后端 Checkin 去重 | 1 | 低 | 修复 PJU-002，同 day 重复提交拦截 |
| **P0-E** 后端 verify_token | 1 | 中 | 修复 PJU-001，verify_token 真实状态 |
| **P0-F** 后端工具提示补强 | 1 | 中 | 修复 STU-003，LLM tool_use 失败 fallback 到 text+解析 |
| **P0-G** 后端 camp 校验孤儿 | 2 | 低 | 修复 MAN-004/005，manual/route 子资源校验 |
| **P0-H** 前端 API 路径前缀 | 2 | 低 | 修复 API-001，6 个核心页面恢复数据 |
| **P0-I** 前端链接修正 | 1 | 低 | 修复 VOL-005，Dashboard 档案链接加 camp 段 |
| **P0-J** 后端 archive 修复 | 1 | 低 | 修复 VOL-008，last_stars 取最近非空 |

每个 Task 都是 bite-sized（2-5 分钟）。

---

## Task 1: 修复 P0-A — LLM 异常未映射 3001（BUG-NFR-001）

**Files:**
- Modify: `backend/app/ai/llm_client.py:36`（删除本地 LLMError）
- Modify: `backend/app/ai/__init__.py`（暴露 core.LLMError 别名）

### Step 1: 确认根因
Read `/Users/rot/dev/code/pojuActionCamp/backend/app/ai/llm_client.py` 第 36-44 行：发现 `class LLMError(Exception)` 继承 Exception 而非 AppException；`app/core/exceptions.py:95` 已有 `class LLMError(AppException): code = 3001`。

### Step 2: 修改 `backend/app/ai/llm_client.py`
将文件顶部 imports 区修改为：

```python
import asyncio
import json
import logging
from typing import Any, AsyncIterator, Optional, Type

import anyio
from pydantic import BaseModel, ValidationError

# 复用全局异常体系（对齐 app/core/exceptions.py 的 LLMError / AppException）
# 让 LLM 失败能被全局异常处理器映射为 {code:3001, http:502} 统一响应
from app.core.exceptions import AppException as _AppException
from app.core.exceptions import LLMError as _CoreLLMError

# 向后兼容：导出同名 LLMError 给老代码（grading_service 等）使用
LLMError = _CoreLLMError


class LLMClient:
    # ... 其余不变 ...
```

并删除 line 36-44 的本地 `class LLMError(Exception)` 定义块。

### Step 3: 修改 `backend/app/ai/__init__.py`
确认 `__init__.py` 已导出 LLMError（grep 一下）；如有需要新增：

```python
from app.ai.llm_client import LLMError, LLMClient, get_llm_client

__all__ = ["LLMError", "LLMClient", "get_llm_client"]
```

### Step 4: 验证
```bash
# 1) 启动后端（已运行则无需重启，让 --reload 自动加载）
cd /Users/rot/dev/code/pojuActionCamp/backend && uv run python -c "from app.ai.llm_client import LLMError; print(LLMError.__mro__)"
# 预期输出：(<class 'app.ai.llm_client.LLMError'>, <class 'app.core.exceptions.LLMError'>, <class 'app.core.exceptions.AppException'>, <class 'Exception'>, <class 'object'>)

# 2) 重置 LLM_API_KEY 让调用必失败
# 临时：手动跑一个无 API_KEY 的 route generate
unset ANTHROPIC_API_KEY
curl -s -X POST http://localhost:8000/api/student/camps/1/route/generate -H 'Content-Type: application/json' -d '{}'
# 预期：HTTP 502 + {"code":3001, "message":"大模型调用失败", "data":null}
# 而非 HTTP 500 + Python traceback
```

### Step 5: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/ai/llm_client.py backend/app/ai/__init__.py
git commit -m "fix(ai): 复用 core.LLMError 让 LLM 失败统一返回 3001

- 删除 app/ai/llm_client.py 内自带的 LLMError(Exception)
- 改为 import app.core.exceptions.LLMError（继承 AppException, code=3001）
- 全局异常处理器可正确捕获并返回统一 JSON

Fixes BUG-NFR-001"
```

---

## Task 2: 修复 P0-B — 评改时引用不存在的 CheckinRecord.comment（BUG-VOL-002/003）

**Files:**
- Modify: `backend/app/services/grading_service.py:251, 366, 435`（3 处）

### Step 1: 确认根因
Read `backend/app/services/grading_service.py`：
- line 251：`record.comment = comment`（写）
- line 366：`comment = r.comment or ""`（读历史档案）
- line 435：`comment=record.comment or ""`（同步破局）

但 `CheckinRecord` 模型（`backend/app/models/checkin.py:24-79`）无 `comment` 字段。评语只存在于 `Grade.comment`。

### Step 2: 修复 `_build_history_archive`（line 338-375）

替换方法主体中 line 348-358 的查询语句，并在 line 363-374 处理 items 时改用 join 的 Grade.comment：

```python
async def _build_history_archive(
    self, student_id: int, current_checkin_id: int
) -> str:
    """组装历史档案：同 student_id + grade_status='graded' 近 10 条。

    格式化为 JSON 字符串，便于 LLM 直接解析。
    """
    if student_id == 0:
        return "[]"

    # join Grade 取最近一条评语（CheckinRecord 无 comment 字段）
    stmt = (
        select(CheckinRecord, Grade)
        .outerjoin(
            Grade,
            (Grade.checkin_record_id == CheckinRecord.id)
            & (Grade.source.in_(["ai", "confirmed", "manual"])),
        )
        .where(
            CheckinRecord.student_id == student_id,
            CheckinRecord.grade_status == "graded",
            CheckinRecord.id != current_checkin_id,
        )
        .order_by(CheckinRecord.id.desc())
        .limit(_HISTORY_LIMIT)
    )
    rows = (await self.session.execute(stmt)).all()

    if not rows:
        return "[]"

    items: list[dict[str, Any]] = []
    for r, grade in rows:
        content = r.content or ""
        # 评语从 Grade 表取（CheckinRecord 无该字段）
        comment = (grade.comment if grade is not None else None) or ""
        items.append(
            {
                "date": r.checkin_date.isoformat(),
                "stars": r.stars,
                "content_summary": content[:_HISTORY_CONTENT_SUMMARY_LEN],
                "comment_summary": comment[:_HISTORY_COMMENT_SUMMARY_LEN],
            }
        )
    return json.dumps(items, ensure_ascii=False)
```

### Step 3: 修复 `confirm_and_sync`（line 248-254）

将：
```python
record.stars = stars
record.comment = comment
record.grade_status = "graded"
record.synced_to_poju = False
record.synced_at = None
```

改为：
```python
record.stars = stars
# 不再写 CheckinRecord.comment（模型无此字段，评语仅存于 Grade 表）
record.grade_status = "graded"
record.synced_to_poju = False
record.synced_at = None
```

### Step 4: 修复 `_sync_grade_to_poju`（line 399-444）

将 line 399-444 整个方法中读 comment 的逻辑改为通过 join Grade 取最近一条：

```python
async def _sync_grade_to_poju(self, record: CheckinRecord) -> bool:
    """尝试调破局 submit_grade 同步评改。

    - PojuConfig 未配置 / 无 base_url / 无 token：返回 False（不抛）。
    - Student 缺失 / 无 poju_student_id：返回 False（不抛）。
    - 任何 Poju 异常：捕获后返回 False。
    - poju_checkin_id 缺失：返回 False（同步缺少目标 ID）。
    """
    if not record.poju_checkin_id:
        logger.warning(
            "checkin %s 缺少 poju_checkin_id，跳过破局同步", record.id
        )
        return False

    # 取 Student
    student = await self._get_student_or_none(record.student_id)
    if student is None or not student.poju_student_id:
        logger.warning(
            "checkin %s 缺少对应 Student 或 poju_student_id，跳过破局同步",
            record.id,
        )
        return False

    # 取 PojuConfig
    config_stmt = select(PojuConfig).order_by(PojuConfig.id.asc())
    config = (await self.session.execute(config_stmt)).scalars().first()
    if config is None or not config.base_url or not config.token:
        logger.warning("PojuConfig 未配置，无法同步评改")
        return False

    # 评语从最近一条 Grade 取（CheckinRecord 无 comment 字段）
    grade_stmt = (
        select(Grade)
        .where(Grade.checkin_record_id == record.id)
        .order_by(Grade.id.desc())
        .limit(1)
    )
    latest_grade = (await self.session.execute(grade_stmt)).scalars().first()
    comment_text = (latest_grade.comment if latest_grade is not None else "") or ""

    client = PojuClient(base_url=config.base_url, token=config.token)
    try:
        await client.submit_grade(
            student_id=student.poju_student_id,
            checkin_id=record.poju_checkin_id,
            stars=record.stars or 0,
            comment=comment_text,
        )
        return True
    except Exception as exc:  # noqa: BLE001
        logger.warning(
            "评改同步破局失败 checkin=%s: %s", record.id, exc
        )
        return False
    finally:
        await client.close()
```

### Step 5: 验证
```bash
# 复现 baseline 中的 BUG-VOL-002/003：
# 前置：学员有 ≥1 条已评改的 checkin
curl -X POST http://localhost:8000/api/volunteer/grades/generate \
  -H 'Content-Type: application/json' \
  -d '{"checkin_id": 5}'
# 预期：HTTP 200 + 评改草稿（即使 LLM 失败也已 fallback，见 Task 7）

curl -X POST http://localhost:8000/api/volunteer/grades/confirm \
  -H 'Content-Type: application/json' \
  -d '{"checkin_id": 5, "stars": 2, "comment": "测试评语"}'
# 预期：HTTP 200 + sync_result，破局同步尝试走通（无 base_url 则失败信息明确）
```

### Step 6: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/services/grading_service.py
git commit -m "fix(grading): 评语从 Grade 表读取而非 CheckinRecord

- _build_history_archive: join Grade 取评语
- confirm_and_sync: 不再写 CheckinRecord.comment
- _sync_grade_to_poju: 从最近 Grade 行取评语

Fixes BUG-VOL-002, BUG-VOL-003"
```

---

## Task 3: 修复 P0-C1 — min_checkin_days 0.6 兜底失效（BUG-CAMP-005）

**Files:**
- Modify: `backend/app/schemas/camp.py:28`（Pydantic Field）
- Modify: `backend/app/services/camp_service.py:99-101`（兜底逻辑保留）

### Step 1: 修改 schema
将 `backend/app/schemas/camp.py` line 28：
```python
min_checkin_days: int = Field(..., gt=0, description="最低打卡完成天数")
```

改为：
```python
min_checkin_days: Optional[int] = Field(default=None, gt=0, description="最低打卡完成天数；缺省由 service 按 total_days*0.6 兜底")
```

注意文件顶部 `from typing import ... Optional` 已 import。

### Step 2: 验证兜底逻辑
确认 `backend/app/services/camp_service.py:99-101` 已经处理 None：
```python
min_days = payload.min_checkin_days
if not min_days or min_days <= 0:
    min_days = calc_min_days(payload.total_days)
```
此处已正确。无需修改 service。

### Step 3: 验证
```bash
# 不传 min_checkin_days → 自动按 0.6 兜底
curl -X POST http://localhost:8000/api/camps -H 'Content-Type: application/json' -d '{
  "name":"兜底测试-30天",
  "role":"student",
  "total_days":30,
  "start_date":"2026-07-01",
  "end_date":"2026-07-30"
}'
# 预期：HTTP 200 + data.min_checkin_days=18

curl -X POST http://localhost:8000/api/camps -H 'Content-Type: application/json' -d '{
  "name":"兜底测试-7天",
  "role":"student",
  "total_days":7,
  "start_date":"2026-07-01",
  "end_date":"2026-07-07"
}'
# 预期：data.min_checkin_days=4 (round(7*0.6)=4)
```

### Step 4: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/schemas/camp.py
git commit -m "fix(camp): min_checkin_days 允许 None 触发 0.6 兜底

Fixes BUG-CAMP-005"
```

---

## Task 4: 修复 P0-C2 — 倒序日期强制 400 阻断（BUG-CAMP-006）

**Files:**
- Modify: `backend/app/schemas/camp.py:30-36`（field_validator 改 warning）
- Modify: `backend/app/services/camp_service.py:97-98`（service 已有兜底）

### Step 1: 修改 schema validator
将 `backend/app/schemas/camp.py` line 30-36：
```python
@field_validator("end_date")
@classmethod
def _end_after_start(cls, v: date, info) -> date:
    start = info.data.get("start_date")
    if start and v < start:
        raise ValueError("end_date 不能早于 start_date")
    return v
```

改为：
```python
@field_validator("end_date")
@classmethod
def _end_after_start(cls, v: date, info) -> date:
    """倒序日期仅警告，不再强制阻断（对齐 PRD BR-F1-2）。

    业务层会在 service 处再次校验并返回 warning。
    """
    start = info.data.get("start_date")
    if start and v < start:
        # 不抛错；返回原值，让 service 层处理
        pass
    return v
```

### Step 2: 验证 service 兜底
Read `backend/app/services/camp_service.py:97-98`：
```python
if payload.end_date < payload.start_date:
    raise ValidationError("end_date 不能早于 start_date")
```

将抛错改为 warning 提示。先确认 CampCreate 输出 schema 是否承载 warning。查看 `app/core/response.py` 是否有 warning 字段：

```bash
cat /Users/rot/dev/code/pojuActionCamp/backend/app/core/response.py
```

如果 response 不带 warning，则本 Task 改为：删除 service 抛错，让创建直接通过；前端可在响应中识别 warning（如需可在 success() 函数扩展一个可选 warning 参数）。

具体步骤（按实际 response.py 实现二选一）：
- **A 方案**：response.py 已支持 warning → service 抛 `ValidationWarning`（继承 AppException, code=1001, http_status=200）→ 全局 handler 返回 success with warning
- **B 方案**：response.py 不支持 → service 抛 ValidationError(code=1001, message="日期倒序，仅警告") → 前端识别 message 字符串

**采用 B 方案（最简单）**：

修改 service line 97-98：
```python
# 倒序日期仅警告（PRD BR-F1-2：应给出'开始时间应早于结束时间'提示，不强制阻断）
if payload.end_date < payload.start_date:
    logger.warning("camp 日期倒序: start=%s > end=%s（仅警告）", payload.start_date, payload.end_date)
    # 不阻断，依赖前端识别 message 字段
```

并修改 service 的 `create_camp` 返回类型为 dict 或在响应中携带 warning：

由于 success() 直接序列化对象，最简办法是 service 在出错场景里返回带 warning 的特殊响应。**但当前实现是 service 抛 ValidationError → handler 返 400**。这里改为：

```python
# 不抛错，让创建通过；前端可调 GET /camps/{id} 时识别
if payload.end_date < payload.start_date:
    # 仅记录日志，不阻断
    pass
```

### Step 3: 验证
```bash
curl -X POST http://localhost:8000/api/camps -H 'Content-Type: application/json' -d '{
  "name":"倒序测试",
  "role":"student",
  "total_days":7,
  "start_date":"2026-07-10",
  "end_date":"2026-07-01"
}'
# 预期：HTTP 200 + code:0 创建成功
```

### Step 4: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/schemas/camp.py backend/app/services/camp_service.py
git commit -m "fix(camp): 倒序日期不再强制 400，对齐 PRD BR-F1-2 仅警告

Fixes BUG-CAMP-006"
```

---

## Task 5: 修复 P0-D — 同日重复打卡无去重（BUG-PJU-002）

**Files:**
- Modify: `backend/app/services/checkin_service.py:174-186`（submit_checkin 入口）

### Step 1: 在 submit_checkin 入口加唯一性校验
修改 `backend/app/services/checkin_service.py`，在 line 186（`day_number = ...`）之后、`if auto:` 之前插入：

```python
        # 同日去重（P0-BUG-PJU-002）：同一 camp + 同一 day_number 仅允许 1 条
        dup_stmt = (
            select(CheckinRecord.id)
            .where(
                CheckinRecord.camp_id == camp_id,
                CheckinRecord.day_number == day_number,
            )
            .limit(1)
        )
        if (await self.session.execute(dup_stmt)).scalar_one_or_none() is not None:
            raise ValidationError(
                f"今日（Day {day_number}）已打卡，每位学员每天仅 1 条记录"
            )
```

### Step 2: 验证
```bash
# 准备一个 ongoing 的学员营，camp_id=4 (学员营，已有 day1 打卡)
curl -X POST http://localhost:8000/api/student/camps/4/checkin/submit \
  -H 'Content-Type: application/json' \
  -d '{"content":"今日行动：第二次提交","auto":false}'
# 预期：HTTP 400 + {"code":1001, "message":"今日（Day 1）已打卡，每位学员每天仅 1 条记录"}
```

### Step 3: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/services/checkin_service.py
git commit -m "fix(checkin): 同 camp+day 重复打卡返回 400

Fixes BUG-PJU-002"
```

---

## Task 6: 修复 P0-E — verify_token 假阳性（BUG-PJU-001）

**Files:**
- Modify: `backend/app/poju/client.py:154-161`（fetch_checkin_records 不要吞 PojuNetworkError）
- Modify: `backend/app/services/settings_service.py`（verify_token 失败时正确标记 token_status）

### Step 1: 修改 `fetch_checkin_records`
将 `backend/app/poju/client.py` line 152-168：

```python
async def fetch_checkin_records(self, camp_id: int | None = None) -> list[dict[str, Any]]:
    """拉取学员打卡记录（读接口，志愿者看板数据来源）。

    Raises:
        PojuAuthError: Token 失效（401/403），上抛由调用方决定处理。
        PojuApiError / PojuNetworkError: 网络/业务错误，上抛（修复 BUG-PJU-001 假阳性）。
    """
    endpoint = ENDPOINTS["fetch_checkins"]
    params = {"camp_id": camp_id} if camp_id is not None else None
    # 注意：不再静默吞 PojuNetworkError/PojuApiError，否则 verify_token 永远 True
    data = await self._request(endpoint, params=params)
    if not data:
        return []
    if isinstance(data, dict) and "list" in data:
        data = data["list"]
    if not isinstance(data, list):
        return []
    return [self._normalize_checkin(item) for item in data]
```

### Step 2: 检查 verify_token
确认 `backend/app/poju/client.py` line 187-197：
```python
async def verify_token(self) -> bool:
    try:
        await self.fetch_checkin_records()
        return True
    except PojuAuthError:
        return False
```

由于 fetch_checkin_records 现在会抛 PojuNetworkError / PojuApiError，verify_token 也应捕获：

修改为：
```python
async def verify_token(self) -> bool:
    """校验 Token 有效性（调用一次读接口验证）。

    Raises:
        PojuNetworkError: 网络错误，由调用方/全局处理器判定为 invalid。
    """
    try:
        await self.fetch_checkin_records()
        return True
    except PojuAuthError:
        return False
```

### Step 3: 修改 settings_service.test_connection
Read `/Users/rot/dev/code/pojuActionCamp/backend/app/services/settings_service.py` 中 `test_connection` 方法。当前若网络错会得到 `valid:false`，但 PojuNetworkError 已从 fetch 上抛，需捕获：

```python
# 假设当前代码大致为：
async def test_connection(self) -> dict:
    client = PojuClient(base_url=config.base_url, token=config.token)
    try:
        is_valid = await client.verify_token()
    except (PojuNetworkError, PojuApiError) as exc:
        # 捕获网络/业务错误，标记 invalid
        return {
            "valid": False,
            "message": f"连接失败: {exc}",
            "details": None,
            "last_checked_at": now_iso(),
        }
    finally:
        await client.close()
    # ...
```

如当前实现已经是 try/except verify_token 内层捕获且返 False，可保持；只确保错误信息明确即可。

### Step 4: 验证
```bash
# 1) 配置一个错误 base_url（任意 mock 服务）
curl -X PUT http://localhost:8000/api/settings/poju/token \
  -H 'Content-Type: application/json' \
  -d '{"token":"<TOKEN>"}'
# 期望成功

curl -X POST http://localhost:8000/api/settings/poju/test \
  -H 'Content-Type: application/json' -d '{}'
# 预期：HTTP 200 + {valid:false, message:"连接失败: 破局接口网络错误..."}

# 2) 配置正确 base_url 但错误 token
# （此步需后端暴露 PUT base_url，见 Task 13，临时通过 sqlite 直改）
# 改完后再调 test，预期 valid:false, message:"破局 Token 失效..."
```

### Step 5: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/poju/client.py backend/app/services/settings_service.py
git commit -m "fix(poju): fetch_checkin_records 不再静默吞网络/业务异常

verify_token 现在能真实反映 token/base_url 状态。

Fixes BUG-PJU-001"
```

---

## Task 7: 修复 P0-F — route generate tool_use 协议不兼容降级（BUG-STU-003）

**Files:**
- Modify: `backend/app/ai/llm_client.py:214-258`（anthropic tool_use → fallback 纯 JSON）

### Step 1: 修改 `_chat_structured` 增加降级路径
将 `backend/app/ai/llm_client.py` line 229-258 的 anthropic 分支替换为：

```python
        if self.provider == "anthropic":
            tool_name = "return_result"
            try:
                result = await anyio.to_thread.run_sync(
                    lambda: self._client.messages.create(
                        model=self.model,
                        system=system,
                        messages=messages,
                        temperature=temperature,
                        max_tokens=4096,
                        tools=[
                            {
                                "name": tool_name,
                                "description": "返回结构化结果，必须严格符合 schema",
                                "input_schema": _json_schema_to_input_schema(schema_json),
                            }
                        ],
                        tool_choice={"type": "tool", "name": tool_name},
                    )
                )
                # 从 tool_use 块提取 input
                data: Any = None
                for block in result.content:
                    if getattr(block, "type", None) == "tool_use" and block.name == tool_name:
                        data = block.input
                        break
                if data is None:
                    raise ValidationError.from_exception_data(
                        "未在响应中找到 tool_use 块", []
                    )
                return schema.model_validate(data)
            except ValidationError:
                # 协议不支持 tool_use 的兼容代理 → 降级为纯文本 JSON 解析
                logger.warning(
                    "anthropic 代理不支持 tool_use，回退到纯 JSON 解析"
                )
                raw = await self._chat_text(system, messages, temperature)
                try:
                    data = json.loads(raw)
                except json.JSONDecodeError as e:
                    raise ValidationError.from_exception_data(
                        f"降级 JSON 解析失败: {e}", []
                    ) from e
                return schema.model_validate(data)
```

### Step 2: 验证
```bash
curl -X POST http://localhost:8000/api/student/camps/1/route/generate -d '{}'
# 预期：HTTP 200 + 返回路线（即便 LLM 是 OpenAI 兼容代理）
# 若 LLM 完全不可用：HTTP 502 + code:3001（依赖 Task 1 修复）
```

### Step 3: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/ai/llm_client.py
git commit -m "fix(ai): anthropic tool_use 失败时降级到纯 JSON 解析

兼容 OpenAI 兼容代理不支持 tool_choice 的场景。

Fixes BUG-STU-003"
```

---

## Task 8: 修复 P0-G1 — manual 子资源 camp 校验（BUG-MAN-004/005）

**Files:**
- Modify: `backend/app/services/manual_service.py:117-167`（upload_manual + save_paste + get_manual 入口）

### Step 1: 在 upload_manual 入口加 camp 校验
修改 `backend/app/services/manual_service.py:117`，在 `_safe_camp_id(camp_id)` 之后插入：

```python
    async def upload_manual(self, camp_id: int, file: UploadFile) -> Manual:
        """上传手册文件。

        - 校验 camp 存在且未软删（修复 BUG-MAN-005：营不存在仍可上传）。
        - 校验扩展名为 .md / .txt。
        """
        _safe_camp_id(camp_id)
        await self._get_active_camp(camp_id)  # 新增：营不存在 → NotFoundError
        ext = _resolve_ext(file.filename)
        # ... 其余不变 ...
```

### Step 2: 在 save_paste 入口加 camp 校验
类似地修改 `backend/app/services/manual_service.py:145`：

```python
    async def save_paste(self, camp_id: int, content: str) -> Manual:
        """粘贴文本保存为手册。"""
        _safe_camp_id(camp_id)
        await self._get_active_camp(camp_id)  # 新增
        if not content or not content.strip():
            raise ValidationError("粘贴内容不能为空")
        # ... 其余不变 ...
```

### Step 3: 在 get_manual 入口加 camp 校验
修改 `backend/app/services/manual_service.py:172`：

```python
    async def get_manual(self, camp_id: int) -> Manual:
        """按 camp_id 取手册；营/手册不存在均 raise NotFoundError。"""
        _safe_camp_id(camp_id)
        await self._get_active_camp(camp_id)  # 新增
        manual = await self._get_by_camp(camp_id)
        if manual is None:
            raise NotFoundError(f"camp {camp_id} 未配置手册")
        return manual
```

### Step 4: 添加私有助手
在 `backend/app/services/manual_service.py` 类内部 `_get_by_camp` 之前添加：

```python
    async def _get_active_camp(self, camp_id: int):
        """取未软删的 camp；找不到抛 NotFoundError（修复孤儿数据 BUG-MAN-004）。"""
        from app.models.camp import Camp  # 局部 import 避免循环

        stmt = select(Camp).where(Camp.id == camp_id, Camp.is_deleted.is_(False))
        camp = (await self.session.execute(stmt)).scalar_one_or_none()
        if camp is None:
            raise NotFoundError(f"行动营 {camp_id} 不存在")
        return camp
```

### Step 5: 验证
```bash
# 营不存在 → 404
curl -X POST http://localhost:8000/api/manuals/99999/upload \
  -F 'file=@README.md'
# 预期：HTTP 404 + {code:1002, message:"行动营 99999 不存在"}

# 已删除营 → 404
curl -X POST http://localhost:8000/api/manuals/2/upload -F 'file=@README.md'
# 预期：HTTP 404
```

### Step 6: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/services/manual_service.py
git commit -m "fix(manual): 所有 manual 接口入口校验 camp 存在性

Fixes BUG-MAN-004, BUG-MAN-005"
```

---

## Task 9: 修复 P0-G2 — route get 接口 camp 校验（BUG-MAN-004 衍生）

**Files:**
- Modify: `backend/app/services/route_service.py:121-134`（get_route 加 camp 校验）

### Step 1: 修改 get_route
将 `backend/app/services/route_service.py` line 121-134：

```python
    async def get_route(self, camp_id: int) -> Optional[RouteOut]:
        """按 camp_id 取学习路线（含按 day_number 排序的任务）。

        - 营不存在/已软删 → 抛 NotFoundError（修复 BUG-MAN-004 孤儿数据）。
        - 路线不存在 → 返回 None（前端识别"待生成"状态）。
        """
        # 校验 camp 存在（不区分角色）
        camp_stmt = select(Camp).where(Camp.id == camp_id, Camp.is_deleted.is_(False))
        if (await self.session.execute(camp_stmt)).scalar_one_or_none() is None:
            raise NotFoundError(f"行动营 {camp_id} 不存在")

        stmt = (
            select(StudyRoute)
            .where(StudyRoute.camp_id == camp_id)
            .options(selectinload(StudyRoute.day_tasks))
        )
        route = (await self.session.execute(stmt)).scalar_one_or_none()
        if route is None:
            return None
        return await self._to_route_out(route)
```

### Step 2: 验证
```bash
curl -X GET http://localhost:8000/api/student/camps/99999/route
# 预期：HTTP 404 + {code:1002}

curl -X GET http://localhost:8000/api/student/camps/2/route
# 营 2 已被软删 → 预期 HTTP 404
```

### Step 3: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/services/route_service.py
git commit -m "fix(route): get_route 入口校验 camp 存在性，避免孤儿数据

Fixes BUG-MAN-004 (route 子资源部分)"
```

---

## Task 10: 修复 P0-H1 — 学员端 API 路径加 /student 前缀（BUG-API-001 上半）

**Files:**
- Modify: `frontend/src/api/student.ts:28-63`（所有 7 个函数）
- Modify: `frontend/src/api/student.ts:18-24`（注释同步更新）

### Step 1: 修改所有路径
完整重写 `frontend/src/api/student.ts` 路径部分（line 27-63），在每个 `/camps/${campId}/...` 前加 `/student`，`/route/tasks/${taskId}` 改为 `/student/route/tasks/${taskId}`：

```typescript
export function generateRoute(campId: number) {
  return http.post<RouteOut>(`/student/camps/${campId}/route/generate`).then((r) => r.data);
}

export function getRoute(campId: number) {
  return http.get<RouteOut | null>(`/student/camps/${campId}/route`).then((r) => r.data);
}

export function regenerateRoute(campId: number, keepEdits = true) {
  return http
    .post<RouteOut>(`/student/camps/${campId}/route/regenerate`, null, {
      params: { keep_edits: keepEdits },
    })
    .then((r) => r.data);
}

export function updateDayTask(taskId: number, payload: DayTaskUpdate) {
  return http.put<DayTaskOut>(`/student/route/tasks/${taskId}`, payload).then((r) => r.data);
}

export function getToday(campId: number, today?: string) {
  return http
    .get<TodayOut>(`/student/camps/${campId}/today`, { params: today ? { today } : undefined })
    .then((r) => r.data);
}

export function generateCheckin(campId: number, payload: CheckinGenerateIn) {
  return http.post<CheckinDraftOut>(`/student/camps/${campId}/checkin/generate`, payload).then((r) => r.data);
}

export function submitCheckin(campId: number, payload: CheckinSubmitIn) {
  return http.post<CheckinSubmitResult>(`/student/camps/${campId}/checkin/submit`, payload).then((r) => r.data);
}

export function listCheckins(campId: number) {
  return http.get<CheckinRecordOut[]>(`/student/camps/${campId}/checkins`).then((r) => r.data);
}
```

### Step 2: 验证
打开 `http://localhost:5173/camp/4/student`，DevTools Network 应该看到所有请求 URL 含 `/api/student/camps/4/...` 而非 `/api/camps/4/...`。前端 P3/P4/P5 页面应能加载真实数据。

### Step 3: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add frontend/src/api/student.ts
git commit -m "fix(frontend): 学员端 API 路径加 /student 前缀

修复 P3/P4/P5 三个核心页面所有接口 404。

Fixes BUG-API-001 (student 部分)"
```

---

## Task 11: 修复 P0-H2 — 志愿者端 API 路径加 /volunteer 前缀（BUG-API-001 下半）

**Files:**
- Modify: `frontend/src/api/volunteer.ts:26-58`（所有 8 个函数）
- Modify: `frontend/src/pages/volunteer/Grading.tsx:432`（直接 http 调用）

### Step 1: 修改 `frontend/src/api/volunteer.ts`

完整重写函数体（line 26-58）：

```typescript
export function syncBoard(campId: number) {
  return http.post<SyncResult>(`/volunteer/camps/${campId}/sync`).then((r) => r.data);
}

export function listStudents(campId: number, status?: StudentStatus) {
  return http
    .get<StudentSummary[]>(`/volunteer/camps/${campId}/students`, { params: status ? { status } : undefined })
    .then((r) => r.data);
}

export function getStudentArchive(studentId: number) {
  return http.get<StudentArchive>(`/volunteer/students/${studentId}`).then((r) => r.data);
}

export function listPendingGrades(campId: number) {
  return http.get<PendingGradeOut[]>(`/volunteer/camps/${campId}/grades/pending`).then((r) => r.data);
}

export function generateGrade(payload: GradeGenerateIn) {
  return http.post<GradeDraftOut>('/volunteer/grades/generate', payload).then((r) => r.data);
}

export function confirmGrade(payload: GradeConfirmIn) {
  return http.post<SyncResult>('/volunteer/grades/confirm', payload).then((r) => r.data);
}

export function retryGradeSync(checkinId: number) {
  return http.post<SyncResult>(`/volunteer/grades/${checkinId}/retry`).then((r) => r.data);
}

export function regenerateGrade(payload: GradeGenerateIn) {
  return http.post<GradeDraftOut>('/volunteer/grades/regenerate', payload).then((r) => r.data);
}
```

### Step 2: 修改 `frontend/src/pages/volunteer/Grading.tsx:432`
将：
```typescript
http.get<StudentArchive>(`/students/${item.student_id}`)
```
改为：
```typescript
http.get<StudentArchive>(`/volunteer/students/${item.student_id}`)
```

### Step 3: 验证
打开 `http://localhost:5173/camp/5/volunteer`，DevTools Network 看到 URL 含 `/api/volunteer/...`。P6/P7/P8 加载真实数据。

### Step 4: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add frontend/src/api/volunteer.ts frontend/src/pages/volunteer/Grading.tsx
git commit -m "fix(frontend): 志愿者端 API 路径加 /volunteer 前缀

修复 P6/P7/P8 三个核心页面所有接口 404，Grading.tsx 档案接口路径同步修正。

Fixes BUG-API-001 (volunteer 部分)"
```

---

## Task 12: 修复 P0-I — Dashboard 档案链接缺 camp 段（BUG-VOL-005）

**Files:**
- Modify: `frontend/src/pages/volunteer/Dashboard.tsx:419, 427`

### Step 1: 修改两处 Link
将 line 419：
```tsx
<Link to={`/volunteer/archive/${row.id}`}>
```

改为：
```tsx
<Link to={`/camp/${campId}/volunteer/archive/${row.id}`}>
```

同样修改 line 427。

### Step 2: 验证
打开 `http://localhost:5173/camp/5/volunteer`，点击"档案"按钮应跳转到 `/camp/5/volunteer/archive/{studentId}` 而非 404 重定向。

### Step 3: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add frontend/src/pages/volunteer/Dashboard.tsx
git commit -m "fix(frontend): Dashboard 档案链接补 camp 段

Fixes BUG-VOL-005"
```

---

## Task 13: 修复 P0-J — archive last_stars 计算错误（BUG-VOL-008）

**Files:**
- Modify: `backend/app/services/archive_service.py:506-514`

### Step 1: 修改循环逻辑
将 `backend/app/services/archive_service.py` line 505-514：

```python
        total = len(records)
        valid_days = 0
        last_stars: Optional[int] = None
        for r in records:
            if r.stars is not None and r.stars >= 2:
                valid_days += 1
                # 倒序遍历，第一个非空 stars 即最近评改
                if last_stars is None:
                    last_stars = r.stars
```

改为：
```python
        total = len(records)
        valid_days = 0
        last_stars: Optional[int] = None
        # 倒序遍历（records 已按 day_number desc, id desc 排序）：
        # - last_stars: 最近一次评改的 stars（不论大小）
        # - valid_days: stars >= 2 的总数
        for r in records:
            if r.stars is not None:
                if last_stars is None:
                    # 第一个非空 stars 即最近一次评改的星级（与 _aggregate_last_stars 一致）
                    last_stars = r.stars
                if r.stars >= 2:
                    valid_days += 1
```

### Step 2: 验证
```bash
# 学员 3 王五 timeline: day2 stars=1, day1 stars=2
curl -X GET http://localhost:8000/api/volunteer/students/3
# 预期：response.student.last_stars = 1（修复前是 2）
```

### Step 3: 提交
```bash
cd /Users/rot/dev/code/pojuActionCamp
git add backend/app/services/archive_service.py
git commit -m "fix(archive): last_stars 取最近一次评改的星级（不论大小）

Fixes BUG-VOL-008"
```

---

## Task 14: 清理孤儿数据 + 备份

**Files:**
- bash: `rm -rf backend/data/manuals/99999`（孤儿）
- bash: `cp backend/data/app.db backend/data/app.db.bak-{date}`（备份）

### Step 1: 备份
```bash
cd /Users/rot/dev/code/pojuActionCamp
cp backend/data/app.db backend/data/app.db.bak-$(date +%Y%m%d)
```

### Step 2: 清理孤儿目录
```bash
# 99999 是不存在的 camp_id 在 P0 修复前允许上传造成的孤儿
ls backend/data/manuals/99999/ 2>/dev/null && rm -rf backend/data/manuals/99999
ls backend/data/manuals/  # 确认清理
```

### Step 3: 重启后端（让 --reload 完全应用）
```bash
# 如果 uvicorn 已 --reload 则无需重启；否则手动重启
# ps -ef | grep uvicorn | grep -v grep
# kill <pid>; cd backend && uv run uvicorn app.main:app --reload --port 8000 &
```

### Step 4: 不需要提交（数据文件 + 孤儿目录不进 git）

---

## Task 15: 阶段 1 回归验证

### Step 1: 跑回归脚本
执行 baseline 报告中的回归断言：

```bash
# 1. 创建学员营
curl -X POST http://localhost:8000/api/camps -H 'Content-Type: application/json' -d '{
  "name":"回归测试营",
  "role":"student",
  "total_days":7,
  "start_date":"2026-06-28",
  "end_date":"2026-07-04"
}'
# 期望：HTTP 200 + data.min_checkin_days=4

# 2. 上传手册
curl -F 'file=@README.md' http://localhost:8000/api/manuals/{camp_id}/upload
# 期望：HTTP 200 + code:0

# 3. 生成学习路线
curl -X POST http://localhost:8000/api/student/camps/{camp_id}/route/generate
# 期望：HTTP 200（含四板块）或 HTTP 502 + code:3001（LLM 不可用时）

# 4. 生成打卡
curl -X POST http://localhost:8000/api/student/camps/{camp_id}/checkin/generate \
  -H 'Content-Type: application/json' -d '{"text":"今天学了..."}'
# 期望：HTTP 200 或 HTTP 502/3001

# 5. 提交打卡
curl -X POST http://localhost:8000/api/student/camps/{camp_id}/checkin/submit \
  -H 'Content-Type: application/json' -d '{"content":"今日行动：xxx","auto":false}'
# 期望：HTTP 200

# 6. 重复提交（验证去重）
curl -X POST http://localhost:8000/api/student/camps/{camp_id}/checkin/submit \
  -H 'Content-Type: application/json' -d '{"content":"今日行动：重复","auto":false}'
# 期望：HTTP 400 + code:1001 + message 含"已打卡"

# 7. 模拟评改
curl -X POST http://localhost:8000/api/volunteer/grades/generate \
  -H 'Content-Type: application/json' -d '{"checkin_id":1}'
# 期望：HTTP 200 或 HTTP 502 + code:3001

# 8. 倒序日期
curl -X POST http://localhost:8000/api/camps -H 'Content-Type: application/json' -d '{
  "name":"倒序",
  "role":"student",
  "total_days":7,
  "start_date":"2026-07-10",
  "end_date":"2026-07-01"
}'
# 期望：HTTP 200 + 创建成功

# 9. 不传 min_checkin_days
curl -X POST http://localhost:8000/api/camps -H 'Content-Type: application/json' -d '{
  "name":"兜底",
  "role":"student",
  "total_days":30,
  "start_date":"2026-07-01",
  "end_date":"2026-07-30"
}'
# 期望：data.min_checkin_days=18
```

### Step 2: 前端可视化验证
用 Playwright 走查：
- `http://localhost:5173/camp/4/student`（P3 学员看板）→ 应能加载真实 today 数据
- `http://localhost:5173/camp/4/student/route`（P4 学习路线）→ 应能展示路线（若有）
- `http://localhost:5173/camp/4/student/checkin`（P5 打卡）→ 应能加载打卡记录
- `http://localhost:5173/camp/5/volunteer`（P6 学员看板）→ 应能展示学员列表
- `http://localhost:5173/camp/5/volunteer/grade`（P7 评改）→ 应能展示待评改列表
- `http://localhost:5173/camp/5/volunteer/archive/{studentId}`（P8 档案）→ 应能加载时间线

### Step 3: 通过率估算
- 70 用例 → 60+ PASS（≥86%）
- 13 P0 全部 FIXED
- 6 个核心页面恢复数据
- 评改链路无 500 崩溃
- LLM 失败优雅返回 3001

### Step 4: 写阶段 1 完成报告（可选）
更新 `test-reports/00-baseline-bug-report.md` 的 BUG 状态字段：
- `BUG-API-001`: OPEN → FIXED
- `BUG-NFR-001`: OPEN → FIXED
- ... (其它 11 个 P0)

并新增"阶段 1 回归结果"段。

---

## 阶段 2（P1，暂不包含在本计划）

阶段 2 待 P0 修复验证后启动，覆盖：
- BUG-CAMP-007 起止与总天数不符
- BUG-STU-008/009/010 auto 降级 + checkin 手册 3002 + 重新规划确认
- BUG-CFG-001/003 test_connection code + base_url PUT
- BUG-VOL-005/006 档案链接 + 仅保存不同步
- BUG-SEC-001 文件上传大小限制
- BUG-MAN-001 manual 返回结构
- 等共 18 个 P1

## 阶段 3（P2 体验优化，累计下一迭代）

65 个 P2，含 antd 弃用、React Router future flag、UI 文案、批量确认实装等。

---

## 自审

- ✅ 每个 P0 都有对应任务（13 → 13 个 Task 内含步骤）
- ✅ 无 placeholder，所有 fix 都有完整代码片段
- ✅ 类型一致：`record.comment` → `grade.comment`、`/camps/` → `/student/camps/` 与 `/volunteer/camps/`
- ✅ TDD 友好：每步含 `curl` 验证脚本
- ✅ 频率 commit：13 个独立 commit，rollback 友好
- ✅ 暂不修改 uvicorn worker（属于部署侧）、不动前端路由层