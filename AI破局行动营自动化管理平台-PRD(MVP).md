# AI破局行动营自动化管理平台 PRD（MVP 版本）

| 项目 | 内容 |
| --- | --- |
| 文档名称 | AI破局行动营自动化管理平台 产品需求文档 |
| 文档版本 | v0.1（MVP，需求澄清稿） |
| 编写日期 | 2026-06-27 |
| 文档状态 | 需求澄清阶段，待评审与修改 |
| 适用范围 | MVP 版本，单用户本地自用 |
| 技术方案 | 本文档不含技术方案设计，仅描述产品需求与业务规则 |

> 说明：本 PRD 为 MVP 阶段需求文档，目标是把需求、边界、业务规则讲清楚。文档不涉及技术选型、架构设计、数据库表结构等实现层面的内容，这些将在后续技术方案阶段单独产出。读者（产品/需求方）阅读本文档后，可对需求进行增删改，确认后再进入技术设计。

---

## 一、项目背景

### 1.1 什么是"AI破局行动营"

"AI破局行动营"是一个定期举办、按主题开展的能力训练社群。每一期行动营围绕一个特定主题（例如某个 AI 应用方向、某项技能训练等）开展一次专项训练，训练周期通常为若干天到数十天不等（如 30 天）。

每一期行动营的典型构成如下：

- **行动手册**：本期训练的核心指导资料，以飞书云文档形式提供，承载训练目标、每日要做的行动、方法论等内容。
- **教练**：负责定期直播教学、在群内答疑。
- **志愿者**：协助运营的工作人员，每名志愿者通常带约 30 名学员，负责开营前添加学员微信并拉群、日常运营、作业评改、对打卡不足的学员及时提醒等。
- **学员**：付费报名参加本期训练的成员，需在训练期间按行动营要求完成指定数量的"打卡"，打卡达到最低天数后可退还押金。

### 1.2 角色职责与规则

本项目涉及的两种核心身份为**学员**与**志愿者**。

#### 学员

- 行动营期间需完成指定数量的打卡，到行动营打卡页面填写打卡内容。
- **有效打卡**：每次打卡作业由志愿者评改，星级范围为 1~3 星，**2 星及以上记为一次"有效打卡"**。
- **退押金条件**：行动营期间累计有效打卡天数达到"最低打卡完成天数"后，可退还押金。
  - 最低打卡完成天数一般约为行动营总天数的 60%。例如 30 天的行动营，需打满 18 天有效打卡。
- 学员需交押金。

#### 志愿者

- 不需交押金，不需打卡，可免费查看手册，但需承担学员运营工作。
- 一名志愿者通常带约 30 名学员。
- 运营工作包括：开营前添加学员微信、拉群；日常打卡通知；直播预告；学员作业评改；对打卡天数不足的学员及时提醒。

### 1.3 项目发起人的处境与痛点

项目发起人（即本平台唯一使用者）同时具有两种身份：在部分行动营中作为**学员**参加，在部分行动营中作为**志愿者**服务。两种身份都存在大量重复性、机械性的工作：

- **作为学员时**：
  - 需要自行研读整本手册，规划每天该做什么，缺乏一个清晰的"今日待办"视图。
  - 每天需手动撰写打卡内容（围绕"今日行动、今日收获、好事分享、下一步行动"等维度），耗时且容易敷衍。
- **作为志愿者时**：
  - 需要每天逐一批改约 30 名学员的作业并打分、写评语，工作量极大且高度重复。
  - 评改时若想判断学员是否进步、是否抄袭之前的作业，需要回忆/翻查该学员的历史作业，缺乏系统化的学员档案。
  - 需要频繁手动查看志愿者看板，了解学员打卡进度。

这些工作高度依赖手册内容、依赖对学员历史表现的掌握、且每天重复发生，非常适合借助大模型 + 自动化来提效。

### 1.4 项目目标

构建一个**本地运行、单用户自用**的自动化管理平台，针对发起人参加的每一期行动营，按其在本期的身份（学员 / 志愿者）提供相应的自动化能力：

- **学员身份**：基于手册自动规划每日学习路线；输入今日实际所做之事，自动生成结构化打卡内容；支持手动或自动提交打卡。
- **志愿者身份**：定时从破局平台拉取志愿者看板数据；为每位学员维护一份贯穿整期的打卡档案；结合手册与学员档案自动生成作业评改结果（星级 + 评语），经人工确认后同步回破局平台。

### 1.5 MVP 定位与价值

MVP 聚焦于把上述两条核心链路（学员打卡内容生成、志愿者作业评改）跑通，并完成行动营管理、手册管理、学员档案、评分标准等基础能力。MVP 明确**不追求**多用户、云端部署、微信自动化等扩展能力，以"个人本地工具、先把重复劳动自动化"为核心价值。

---

## 二、名词解释

| 名词 | 含义 |
| --- | --- |
| 行动营 | 一期围绕特定主题的专项训练，有起止时间、总天数、最低打卡完成天数等属性 |
| 手册 | 行动营的核心指导资料，飞书云文档形式，平台中作为 AI 生成/评改的参考上下文 |
| 打卡 | 学员每日提交的训练记录，包含结构化内容，由志愿者评改后给出 1~3 星 |
| 有效打卡 | 评改星级 ≥ 2 星的打卡，计入"有效打卡天数" |
| 最低打卡完成天数 | 学员需达到的有效打卡天数门槛，达到后可退押金，一般约为总天数 × 60% |
| 破局平台 | "AI破局行动营"的官方平台，提供打卡提交、志愿者看板等能力，通过接口与之交互 |
| 破局接口 | 通过浏览器抓包获取的破局平台内部接口，需携带 Authorization 请求头鉴权 |
| 志愿者看板 | 破局平台提供给志愿者的管理视图，含所辖学员的身份信息、打卡记录、已打卡天数等 |
| 学员档案 | 平台内为"每位学员 × 每期行动营"维护的历史打卡记录集合，供 AI 评改时参考，学员不可见 |
| 学习路线 | 学员身份下，AI 根据手册内容与行动营总天数/起止时间规划出的每日任务安排 |
| 评改 | 志愿者对学员打卡作业进行打分（星级）与评价（评语）的动作 |

---

## 三、范围定义

### 3.1 MVP 纳入范围

| 模块 | 是否纳入 | 说明 |
| --- | --- | --- |
| 行动营创建与管理 | ✅ | 创建行动营、选择身份、录入基本信息 |
| 手册内容管理 | ✅ | 手动上传手册文本内容（与获取手段解耦） |
| 学员身份 · 学习路线规划 | ✅ | AI 根据手册规划每日任务，提供每日看板 |
| 学员身份 · 打卡内容生成 | ✅ | 输入今日所做（文字+图片），生成结构化打卡内容 |
| 学员身份 · 打卡提交 | ✅ | 手动/自动提交开关（自动提交依赖破局写接口） |
| 志愿者身份 · 看板数据拉取 | ✅ | 定时调用破局接口拉取志愿者看板信息 |
| 志愿者身份 · 作业评改生成 | ✅ | 结合手册+学员档案自动生成评改结果，人工确认后同步 |
| 学员档案管理 | ✅ | 每位学员×每期行动营一份档案，记录历史打卡 |
| 评分标准管理 | ✅ | 内置初版可配置评分标准，支持后续补充 |
| Token 配置 | ✅ | 手动配置破局接口 Authorization Token |

### 3.2 MVP 明确不纳入范围（排除项）

| 模块 | 是否纳入 | 原因 / 后续考虑 |
| --- | --- | --- |
| 用户系统 / 账号体系 | ❌ | MVP 单用户自用，无需账号；预留多用户扩展能力 |
| 云端部署 | ❌ | MVP 本地运行 |
| 微信自动化（拉群、发通知、提醒） | ❌ | 日常打卡通知、直播预告、打卡不足提醒等均在微信侧完成，且有封号风险与复杂度；MVP 不做 |
| 手册自动 OCR / 飞书抓取 | ❌ | 飞书文档经破局自定义域名封装，无导出/复制能力；OCR 与主流程解耦，由用户自行准备手册文本后上传 |
| 自动登录破局平台 / 自动获取 Token | ❌ | MVP 手动登录后配置 Token |
| 教练相关功能 | ❌ | 本平台使用者不承担教练角色 |
| 学员本人查看自己的档案 | ❌ | 学员档案仅志愿者侧维护，学员不可见 |
| 多身份并发（同一期既是学员又是志愿者） | ❌ | 单期行动营创建时选定一个主身份；不同行动营可分别选不同身份 |

---

## 四、前置依赖与约束

> 以下依赖项是 MVP 能否完整跑通的前提，需在开发前/中逐项确认。任何一项缺失都会触发对应功能的降级方案（见第九章）。

### 4.1 破局接口依赖

- **来源**：通过浏览器抓包获取的破局平台内部接口，所有接口需携带 `Authorization` 请求头鉴权。
- **鉴权方式**：MVP 由用户手动登录破局平台后获取 Token，并在平台内手动配置；不做自动登录与自动获取 Token。
- **接口能力**：基于发起人上一期使用 Python 脚本自动化的经验，至少存在以下接口：
  - **读接口**：拉取学员打卡记录（志愿者看板相关数据）。
  - **写接口**：给学员作业打分与评价。
- **待确认项**：最新版志愿者看板的接口尚未开放，开放后由发起人提供详细接口定义（请求/响应字段、列表分页、学员标识等）。MVP 开发需以最终接口定义为准对齐字段。
- **风险**：内部抓包接口存在变动风险；Token 存在过期风险。需在 PRD 与后续技术方案中考虑失效检测与提示。

### 4.2 手册内容依赖

- 手册为飞书云文档，经破局自定义域名封装，平台内**无导出/复制能力**，仅能通过浏览器插件 OCR 识别。
- MVP 将"手册获取"与"主流程"**解耦**：平台不负责抓取/OCR，仅提供"上传手册文本内容"的能力。用户自行通过任意方式（OCR、手敲、整理）得到手册的文本（md/txt），上传到对应行动营目录下，作为 AI 生成打卡内容 / 评改作业的参考上下文。

### 4.3 运行环境约束

- MVP 为本地运行，面向发起人个人使用，不部署到服务器。
- 需支持在本地运行**定时任务**（用于志愿者侧定时拉取看板数据等）。
- 需调用大模型能力（用于学习路线规划、打卡内容生成、作业评改），具体模型与调用方式在技术方案阶段确定。

---

## 五、用户角色与使用场景

本平台为单用户（发起人）工具，"用户角色"指发起人在某期行动营中所承担的身份。同一用户在不同行动营中可能承担不同身份。

### 5.1 场景一：作为学员参加某期行动营

1. 创建行动营，身份选择"学员"，录入基本信息，上传手册文本。
2. 平台基于手册与行动营日程，自动规划整期学习路线（每日任务）。
3. 每天打开平台看板，查看"今日该做什么"。
4. 在平台输入今日实际所做之事（文字 + 图片），平台自动生成结构化打卡内容（今日行动 / 今日收获 / 好事分享 / 下一步行动）。
5. 检查生成内容，按需修改；通过手动复制到破局打卡页面提交，或在开启自动提交时由平台代为提交。

### 5.2 场景二：作为志愿者服务某期行动营

1. 创建行动营，身份选择"志愿者"，录入基本信息，上传手册文本。
2. 配置破局接口 Token。
3. 平台定时拉取志愿者看板数据，展示所辖全部学员的身份信息、打卡记录、已打卡天数等。
4. 平台为每位学员自动建立/维护一份学员档案（记录历史打卡）。
5. 针对每天学员提交的新打卡，平台结合手册内容 + 该学员档案（历史作业）自动生成评改结果（星级 + 评语）。
6. 志愿者逐条确认评改结果，确认后平台调用破局写接口同步到破局平台。
7. （后续可演进）开启自动评改/自动同步，减少人工确认环节。

---

## 六、功能需求

### F1 行动营管理

**功能描述**：创建并管理发起人参加的每一期行动营，创建时选定本期身份，并录入基本信息。

**F1.1 创建行动营**

创建时需填写/选择以下信息：

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| 行动营名称 | 文本 | 是 | 如"XX主题破局行动营" |
| 身份 | 枚举（学员 / 志愿者） | 是 | 本期发起人的身份，单期仅一个主身份 |
| 简介 | 长文本 | 否 | 行动营主题/目标简述 |
| 总天数 | 整数 | 是 | 行动营训练总天数，如 30 |
| 开始时间 | 日期 | 是 | 行动营开始日期 |
| 结束时间 | 日期 | 是 | 行动营结束日期（应与开始时间+总天数一致，平台做合理性校验并提示） |
| 最低打卡完成天数 | 整数 | 是 | 有效打卡达此天数可退押金，如 18；默认建议值 = 总天数 × 0.6，可修改 |

**业务规则**：
- BR-F1-1：单期行动营身份一经创建不可更改（若选错，建议删除重建；MVP 不支持中途切换身份）。
- BR-F1-2：开始时间应早于结束时间；总天数应与起止日期跨度相符，不符时给出提示但不强制阻断。
- BR-F1-3：最低打卡完成天数默认取 `总天数 × 0.6` 四舍五入，允许手动修改。
- BR-F1-4：行动营状态随时间推进自动流转（未开始 / 进行中 / 已结束），作为看板与提醒的逻辑依据。

**F1.2 行动营列表与详情**

- 提供行动营列表，展示名称、身份、状态、起止时间、进度等信息。
- 支持进入单个行动营详情，详情视图根据身份不同，分别进入"学员工作台"或"志愿者工作台"。
- 支持删除行动营（MVP 可软删除或直接删除，删除时提示将一并删除其下手册、学习路线/学员档案等数据）。

---

### F2 学员身份功能

适用于身份为"学员"的行动营。

**F2.1 学习路线规划**

**功能描述**：基于手册内容 + 行动营总天数与起止时间，由 AI 自动规划整期学习路线，拆解为每日任务。

**输入**：手册文本内容、行动营总天数、开始时间、结束时间。

**输出**：按天排列的任务清单（Day1 ~ DayN），每天包含建议的任务/行动要点。

**业务规则与说明**：
- BR-F2.1-1：手册本身**无固定按天结构**，需由 AI 根据总天数与内容量**自主规划**每日任务，而非简单提取。
- BR-F2.1-2：规划结果可由用户手动编辑调整（增删改每日任务），编辑后以用户版本为准。
- BR-F2.1-3：支持"重新规划"（基于手册重新生成），重新规划会覆盖 AI 生成部分，但应提示是否保留用户已编辑的内容（MVP 可简化为：重新规划前提示将覆盖，由用户自行决定）。
- BR-F2.1-4：学习路线生成后持久化存储，作为每日看板的数据来源。

**F2.2 每日看板（今日该干什么）**

**功能描述**：学员每日打开平台即可看到今天对应的任务安排。

**业务规则**：
- BR-F2.2-1：根据当前日期与行动营开始时间，计算今天是第几天（Day N），展示学习路线中对应天的任务。
- BR-F2.2-2：若当前日期早于开始时间，提示"行动营尚未开始"；晚于结束时间，提示"行动营已结束"并展示总结性信息（如完成情况）。
- BR-F2.2-3：看板可展示本期整体进度（当前天数 / 总天数）与打卡进度（已有效打卡天数 / 最低打卡完成天数，注：学员侧的打卡进度若破局接口可读则展示，不可读则仅展示任务进度）。
- BR-F2.2-4：允许查看历史天（过去某天）与未来天（后续任务预览）的任务。

**F2.3 打卡内容生成**

**功能描述**：学员输入今日实际所做之事（文字 + 图片），平台自动生成符合模板的结构化打卡内容。

**输入**：
- 今日所做之事的文字描述（必填）。
- 相关图片（选填，作为生成参考）。

**输出**：按以下模板生成的打卡内容：

| 板块 | 说明 |
| --- | --- |
| 今日行动 | 今天具体做了什么 |
| 今日收获 | 从中获得的收获/感悟 |
| 好事分享 | 今天值得分享的一件好事 |
| 下一步行动 | 接下来打算做什么 |

**业务规则**：
- BR-F2.3-1：打卡内容**无强制格式/字数要求**，由 AI 根据用户输入的实际内容生成，体现真实情况。
- BR-F2.3-2：生成内容可由用户编辑修改后再提交。
- BR-F2.3-3：可参考学习路线中今日任务与手册相关内容，使打卡内容贴合本期主题。
- BR-F2.3-4：支持针对同一天多次生成/调整，保留最近一次为准（历史版本是否留存由技术方案决定，MVP 不强制）。

**F2.4 打卡提交**

**功能描述**：将最终确认的打卡内容提交到破局平台。

**业务规则**：
- BR-F2.4-1：提供"手动提交 / 自动提交"**开关**。
  - **手动提交（默认）**：用户确认生成内容后，平台不自动提交；用户自行复制内容到破局打卡页面提交。前期用于把控质量、调整提示词。
  - **自动提交**：开关开启后，平台在用户确认生成内容后，调用破局接口自动提交打卡。
- BR-F2.4-2：自动提交依赖破局"提交打卡"写接口；若该接口不可用，则强制走手动提交，并在界面明确提示。
- BR-F2.4-3：提交动作（无论手动复制还是自动提交）应记录提交状态与时间，便于追踪是否已打卡。

---

### F3 志愿者身份功能

适用于身份为"志愿者"的行动营。

**F3.1 破局接口 Token 配置**

**功能描述**：用户手动登录破局平台获取 Token 后，在平台内配置，供接口调用鉴权。

**业务规则**：
- BR-F3.1-1：Token 以请求头 `Authorization` 形式携带；存储方式在技术方案阶段确定，需注意本地存储安全性。
- BR-F3.1-2：支持更新 Token（Token 过期或失效时更换）。
- BR-F3.1-3：调用接口遇鉴权失败（如 401）时，平台提示"Token 可能已失效，请更新"。

**F3.2 志愿者看板数据拉取**

**功能描述**：定时调用破局接口拉取志愿者看板信息，获取所辖全部学员的相关数据。

**拉取内容（以最终接口定义为准，预期包含）**：
- 学员身份信息（昵称、唯一标识等）。
- 学员打卡记录（每日打卡内容、提交时间、当前评改星级等）。
- 学员已打卡天数 / 有效打卡天数等进度数据。

**业务规则**：
- BR-F3.2-1：支持**定时自动拉取**（本地定时任务，频率在技术方案阶段确定，如每日固定时间 + 可手动触发）。
- BR-F3.2-2：支持**手动触发拉取**（立即刷新）。
- BR-F3.2-3：拉取的数据持久化存储，作为学员档案与评改的数据来源。
- BR-F3.2-4：拉取失败（网络/鉴权/接口异常）时记录失败原因并提示，不影响已有数据展示。
- BR-F3.2-5：学员数据按"学员唯一标识"去重对齐，跨多次拉取保持同一学员的档案连续性。

**F3.3 志愿者看板展示**

**功能描述**：展示所辖全部学员的当前状态，便于志愿者掌握整体情况。

**展示内容（预期）**：
- 学员列表：昵称、已打卡天数、有效打卡天数、距最低打卡完成天数差距、最近一次打卡时间与星级等。
- 单个学员详情：该学员本期全部打卡记录与评改结果（即学员档案的可视化展示）。
- 整体统计：如已评改/待评改数量、各星级分布等（MVP 提供基础统计即可）。

**业务规则**：
- BR-F3.3-1：待评改的打卡应突出展示，便于志愿者快速定位工作。
- BR-F3.3-2：可按"待评改 / 已评改 / 打卡不足"等维度筛选查看。

**F3.4 作业评改生成**

**功能描述**：针对学员每日提交的打卡，平台结合手册内容与该学员档案（历史作业），由 AI 自动生成评改结果（星级 + 评语），供志愿者确认。

**输入**：
- 待评改的学员打卡内容。
- 本期手册内容（作为评改参照）。
- 该学员在本期的历史打卡档案（用于判断进步、原创性等）。
- 当前生效的评分标准（见 F5）。

**输出**：
- 星级（1~3 星）。
- 评语（文字评价）。
- （可选）评改依据/维度得分说明，便于志愿者理解 AI 给分的理由。

**业务规则**：
- BR-F3.4-1：评改**必须参考该学员历史作业档案**，重点判断：是否抄袭/重复之前作业、相比之前是否有进步、内容真实性等。
- BR-F3.4-2：评改结果可由志愿者编辑修改（改星级、改评语）后再确认同步。
- BR-F3.4-3：志愿者确认后，平台调用破局"给学员作业打分和评价"写接口，将星级与评语同步到破局平台。
- BR-F3.4-4：同步成功后，该条打卡标记为"已评改"，并写入学员档案。
- BR-F3.4-5：同步失败时保留评改结果不丢失，提示失败原因并支持重试。
- BR-F3.4-6：支持批量确认/同步（MVP 至少支持逐条；批量可作为增强）。
- BR-F3.4-7：（后续可演进）提供"自动评改 + 自动同步"开关，成熟后减少人工确认；MVP 默认人工确认。

**F3.5 学员档案维护**

**功能描述**：为每位学员 × 每期行动营自动建立并维护一份档案，记录其本期全部打卡与评改历史，作为后续评改的参考依据。

**业务规则**：
- BR-F3.5-1：档案粒度为"学员 × 行动营"，即同一学员在不同行动营有各自独立的档案。
- BR-F3.5-2：档案随看板数据拉取与评改同步自动更新，无需手动维护。
- BR-F3.5-3：档案内容至少包含：学员标识、各次打卡内容、对应星级与评语、提交与评改时间。
- BR-F3.5-4：学员档案**仅志愿者侧可见**，学员本人不可见。
- BR-F3.5-5：档案为 AI 评改提供上下文，调用大模型时需将相关历史作业纳入提示（具体实现见技术方案）。

---

### F4 手册内容管理

**功能描述**：管理各行动营的手册文本内容，作为 AI 生成与评改的参考上下文。

**业务规则**：
- BR-F4-1：手册获取与主流程**解耦**，平台不负责 OCR/抓取，仅提供上传入口。
- BR-F4-2：支持上传手册文本文件（md / txt），或直接粘贴文本内容，存入对应行动营目录下。
- BR-F4-3：支持替换/更新手册内容（行动营期间手册可能更新）。
- BR-F4-4：手册内容持久化存储，供学习路线规划、打卡内容生成、作业评改等场景作为上下文读取。
- BR-F4-5：手册为选填但强烈建议填写；未上传手册时，相关 AI 功能可降级运行（仅基于用户输入生成，质量下降并提示）。

---

### F5 评分标准管理

**功能描述**：管理作业评改的评分标准，供 AI 评改时遵循。MVP 内置一套初版可配置评分标准，并预留后续补充入口。

**F5.1 内置初版评分标准（建议，可调整）**

打卡内容模板含四个板块（今日行动 / 今日收获 / 好事分享 / 下一步行动），评分维度建议如下：

| 评分维度 | 说明 |
| --- | --- |
| 完整度 | 四个板块是否均有填写、内容是否完整 |
| 行动真实性 | 今日行动是否为真实完成的具体行动，而非空泛套话 |
| 收获深度 | 今日收获是否有具体感悟/思考，而非流水账 |
| 进步性 | 相比该学员历史作业是否有进步/变化（依赖学员档案） |
| 原创性 | 是否抄袭/重复本人之前作业或他人作业（依赖学员档案） |

星级判定规则（建议）：

| 星级 | 判定 | 是否有效打卡 |
| --- | --- | --- |
| ⭐⭐⭐ 三星 | 优秀：各维度均达标，收获有深度，且有明显进步/亮点 | ✅ 有效 |
| ⭐⭐ 二星 | 良好：基本达标，内容完整真实，无抄袭 | ✅ 有效 |
| ⭐ 一星 | 待改进：内容敷衍、不完整、套话、抄袭或重复之前作业 | ❌ 无效 |

**业务规则**：
- BR-F5-1：评分标准（维度、维度说明、星级判定规则）以**可配置**形式存储，支持编辑修改。
- BR-F5-2：评分标准作用于评改提示词，作为 AI 打分的依据。
- BR-F5-3：MVP 内置上述初版标准，用户可在此基础上增删维度、调整判定规则；后续可补充更细化的标准。
- BR-F5-4：评分标准的修改对后续评改生效；已评改记录不回溯重评（除非用户主动重新评改）。

---

### F6 Token 与接口配置

**功能描述**：集中管理破局接口调用所需的配置（Token 等）。

**业务规则**：
- BR-F6-1：支持配置与更新 Authorization Token。
- BR-F6-2：接口调用统一携带 Token，鉴权失败时统一提示更新。
- BR-F6-3：预留接口配置扩展能力（如接口地址、其他请求头等），便于破局接口定义明确后调整。

---

## 七、核心业务实体与关系（概念级）

> 本节为业务概念梳理，不涉及数据库设计。仅描述关键业务对象及其关系，帮助理解功能边界。

- **用户**：MVP 单一用户（发起人），可参加多期行动营。
- **行动营**：一期训练，含基本信息（名称、身份、天数、起止时间、最低打卡天数）与状态。
  - 关联一份**手册内容**。
  - 身份为学员时，关联一份**学习路线**（每日任务）。
  - 身份为志愿者时，关联多个**学员档案**。
- **手册内容**：归属于某行动营，作为 AI 上下文。
- **学习路线**：归属于某学员身份的行动营，由多日任务组成。
- **每日任务**：学习路线中某一天的安排。
- **学员档案**：归属于某志愿者身份的行动营 × 某学员，含多条打卡评改记录。
- **打卡评改记录**：某学员某天的一次打卡及其评改结果（星级、评语、状态）。
- **评分标准**：全局可配置（MVP 可全平台一套，也可按行动营覆盖；建议 MVP 全局一套）。
- **接口配置**：全局 Token 等配置。

关系简述：
```
用户 1 — N 行动营
行动营 1 — 1 手册内容
行动营(学员) 1 — 1 学习路线 1 — N 每日任务
行动营(志愿者) 1 — N 学员档案 1 — N 打卡评改记录
全局: 评分标准、接口配置
```

---

## 八、业务规则汇总（跨功能）

- BR-G-1：身份决定可用功能。学员身份行动营仅可见学员工作台（F2）；志愿者身份行动营仅可见志愿者工作台（F3）。
- BR-G-2：有效打卡 = 评改星级 ≥ 2 星；最低打卡完成天数默认 = 总天数 × 0.6（可改）。
- BR-G-3：学员档案粒度为"学员 × 行动营"，跨期独立、跨期不可见学员本人。
- BR-G-4：评改必须参考学员历史档案；学习路线规划必须基于手册自主规划（手册无固定结构）。
- BR-G-5：所有破局接口调用依赖手动配置的 Token；鉴权失败统一提示更新 Token。
- BR-G-6：AI 生成内容（学习路线、打卡内容、评改结果）均支持人工编辑修改，以人工确认版本为准。
- BR-G-7：写入破局平台的动作（提交打卡、同步评改）失败时不丢失本地数据，支持重试。

---

## 九、降级与容错设计

针对前置依赖的不确定性，明确各功能的降级方案，确保主流程在依赖缺失时仍可使用（即便能力减弱）。

| 依赖缺失场景 | 影响功能 | 降级方案 |
| --- | --- | --- |
| 破局**读**接口不可用 / Token 失效 | F3.2 看板拉取、F3.3 看板展示、F3.5 档案自动更新 | 提示接口异常，停止自动拉取；保留已拉取数据；支持手动录入/补录学员与打卡信息（MVP 可简化为提示 + 等待恢复） |
| 破局**写**接口（提交打卡）不可用 | F2.4 自动提交 | 强制走手动提交：生成内容后由用户复制到破局页面，平台仅记录"已手动提交"状态 |
| 破局**写**接口（同步评改）不可用 | F3.4 评改同步 | 评改结果在平台内生成与确认，由用户手动复制星级/评语到破局页面；平台记录结果，待接口恢复后可补同步 |
| 手册未上传 | F2.1 学习路线、F2.3 打卡生成、F3.4 评改 | 相关 AI 功能降级运行（仅基于用户输入/学员打卡本身生成，质量下降并提示"未配置手册，建议上传"） |
| 大模型调用失败 | 所有 AI 生成功能 | 提示生成失败并支持重试；不影响已有数据与人工操作 |
| Token 过期 | 所有破局接口调用 | 统一提示"Token 可能失效，请更新"；更新后恢复 |

---

## 十、非功能需求

| 维度 | 要求 |
| --- | --- |
| 使用规模 | MVP 单用户本地自用 |
| 运行环境 | 本地运行，支持本地定时任务常驻 |
| 数据持久化 | 行动营、手册、学习路线、学员档案、评改记录、配置等需持久化存储于本地 |
| 安全性 | Token 等敏感信息本地存储需注意安全（具体方式技术方案确定）；不对外暴露 |
| 可用性 | 接口/AI 调用失败需有明确提示与重试，不静默失败、不丢失数据 |
| 可维护性 | 评分标准、提示词、接口配置等可配置，便于调优与适配破局接口变更 |
| 扩展性（预留） | 预留多用户、云端部署、多身份并发、微信自动化等扩展能力，但 MVP 不实现 |

---

## 十一、后续演进方向（MVP 之外，预留能力）

以下为 MVP 之后的演进方向，MVP 不实现但设计时预留扩展空间：

1. **多用户体系**：支持其他志愿者/学员注册使用，需账号、权限、数据隔离。
2. **云端部署**：从本地工具演进为可常驻云端的平台，支持稳定定时任务与多端访问。
3. **自动评改与自动提交**：提示词与评分标准成熟后，开启全自动闭环，减少人工确认。
4. **手册自动获取**：集成浏览器自动化 + OCR，实现手册一键转 md（当前因飞书封装限制，由用户手动准备）。
5. **运营提醒自动化**：打卡通知、直播预告、打卡不足提醒等（当前走微信人工）。
6. **同一期多身份**：支持同一期行动营中既是学员又是志愿者。
7. **数据看板与复盘**：跨行动营的数据统计、学员进步趋势、个人复盘报告等。
8. **评分标准精细化**：分主题/分行动营的差异化评分标准、评分一致性校准等。

---

## 十二、待确认事项（开放问题）

以下问题在开发前需进一步确认，确认后更新本文档：

1. **破局接口定义**：最新版志愿者看板接口开放后，需提供详细接口文档（读/写接口的请求方法、路径、请求头、请求参数、响应字段、分页方式、学员唯一标识字段等），以对齐 F3 相关功能的数据字段。
2. **提交打卡接口**：是否存在"学员提交打卡"的写接口（用于 F2.4 自动提交），还是学员打卡只能在破局页面手动提交？若不存在，自动提交开关将永久降级为手动。
3. **学员打卡进度可见性**：学员身份下，能否通过破局接口读取自己的有效打卡天数 / 评改星级（用于 F2.2 看板展示打卡进度）？若不可读，学员看板仅展示任务进度。
4. **Token 有效期**：破局 Token 的有效期时长与失效表现，影响 Token 失效检测与提示策略。
5. **图片处理**：F2.3 学员输入的图片，是仅作为生成参考，还是需要随打卡内容提交到破局平台（破局打卡是否支持图片）？
6. **评分标准初版**：本文档 F5 提供的初版评分标准是否认可，或是否有既有的评分标准可替换。
7. **行动营删除策略**：MVP 行动营删除是软删除（可恢复）还是硬删除，是否需二次确认。

---

## 十三、附录：打卡内容模板

学员打卡内容统一包含以下四个板块（无强制格式与字数要求，依据实际情况填写）：

| 板块 | 含义 |
| --- | --- |
| 今日行动 | 今天具体做了什么 |
| 今日收获 | 从中获得的收获/感悟 |
| 好事分享 | 今天值得分享的一件好事 |
| 下一步行动 | 接下来打算做什么 |

---

> 文档结束。请阅读后对需求进行增删改，特别关注**第四章前置依赖**与**第十二章待确认事项**，确认后即可进入技术方案设计阶段。
